server:
  port: 9000

spring:
  application:
    name: api-gateway
  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}
  cloud:
    config:
      uri: http://localhost:8888
